#!/bin/bash
has() { type -p "$1" >/dev/null; }
if has lsb_release ; then
    distro=$(lsb_release -s)
elif has brew ; then
    distro="Mac OS"
elif has termux-open ; then
    distro="Termux"
else
    distro=$(uname -s)
fi
printf "Good day, welcome to $distro!\n"
neofetch

